# Microsoft Agent Framework (Foundry client lives in agent_framework.foundry).
# The framework is in active preview - if a plain install cannot find it, use:
#   pip install --pre agent-framework
agent-framework
azure-identity

# API
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
